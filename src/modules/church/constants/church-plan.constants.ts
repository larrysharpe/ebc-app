export const CHURCH_PLAN_ID = 'default';
/** Max characters per ministry plan excerpt in the church AI prompt. */
export const CHURCH_PLAN_EXCERPT_MAX_CHARS = 500;
/** Refresh church plan when older than this (hours). */
export const CHURCH_PLAN_MAX_AGE_HOURS = 24;
